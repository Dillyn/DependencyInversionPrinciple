package ac.za.cput;

import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.*;

public class BackEndDeveloperTestTest {
    @Test
    public void dev(){

        BackEndDeveloper backDev = new BackEndDeveloper();

        /*Hello sir. I could not think of a way to test this, but I have something here.*/
        Assert.assertEquals(, backDev.develop());
    }

}